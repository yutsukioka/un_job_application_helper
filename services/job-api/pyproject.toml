[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "job-api"
version = "0.1.0"
description = "Local HTTP API over jobagg for the Mac/iOS search app."
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115",
  "uvicorn>=0.30",
  "jobagg",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
  "httpx>=0.27",
]

[project.scripts]
job-api = "job_api.app:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["job_api*"]
