interface:
  display_name: "Apex ▸ JD Core Requirements"
  short_description: "Extract the 5-7 most critical requirements AND any knockout criteria; include star weights where available."
  default_prompt: |
    Use this skill to extract knockout criteria and the top 5-7 core requirements from the JD.
    Follow AGENTS.md, apex-guardrails, and apex-jd-core-requirements/SKILL.md.
    Treat apex-jd-core-requirements/SKILL.md as the canonical behavior and output contract.
