# Apple App

Placeholder for the shared SwiftUI Mac/iOS app.

Implementation starts after the `services/job-api` contract stabilizes. The app
must use the HTTP API and must not read SQLite bundles or scrape ATS sites
directly.

Planned shared client modules:

- API client and DTOs for `contracts/api`
- Search and filter state
- Saved-search state
- Tracker state
- Strategy-fit and assistant run views
